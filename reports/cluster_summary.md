# Cluster Optimization Summary

We evaluated K-Means clustering for K between 2 and 10.
Using the **Silhouette Score** optimization, the mathematically optimal number of clusters is **K=2**, achieving the highest intra-cluster cohesion and inter-cluster separation. 
The WCSS (Elbow method) curve also supports this inflection point.
