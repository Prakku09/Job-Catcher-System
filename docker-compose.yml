version: '3.8'

services:
  job-catcher-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
